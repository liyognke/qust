spring:
  datasource:
    url: jdbc:mysql://localhost:3306/test?
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.transport.bean
#server:
#  servlet:
#    context-path: /  #//上下文路径
